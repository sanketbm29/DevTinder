<!-- git add all files -->
git add .

<!-- git commit -->
git commit -m "Message"

<!-- rename the current branch to main -->
git branch -M main

<!-- pushes your local main branch to the remote repository -->
git push -u origin main

<!-- setting up a new local repository and connecting it to GitHub -->
git remote add origin https://github.com/sanketbm29/DevTinder.git